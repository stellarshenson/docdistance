# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - Project initialization** (v0.1.0): Initialized Claude Code configuration for the `docdistance-estimator` project scaffolded from copier-data-science v1.3.9<br>
   **Result**: Created `.claude/CLAUDE.md` importing workspace-level configuration from `/home/lab/workspace/.claude/CLAUDE.md` with the mandatory reinforced bans (no auto tags/version/publish/commits, Makefile-first installs) and the append-only journal rules. Added project context describing the Word Mover's Distance / Optimal Transport purpose - meaningful document distance for agentic conversion/extraction pipelines that run through frontier models without access to logits for KL divergence. Documented the stack (Python 3.13, `uv`, `typer`, `loguru`, `ruff`, `pytest`) and strengthened rules for notebook standards, background-job logging, and optimal-transport metric correctness. Project already had a local git repo from copier (no commits yet), so git init was skipped.

2. **Task - WMD paper digest** (v0.1.0): Processed the source PDF `[paper] From Word Embeddings To Document Distances.pdf` into a markdown digest<br>
   **Result**: Extracted all 10 pages of the Kusner et al. 2015 ICML paper with `pdfplumber` (poppler was unavailable for direct PDF rendering) and wrote `references/papers/from-word-embeddings-to-document-distances.md` following modus primaris. The digest covers the BOW/TF-IDF distance problem, the nBOW representation, the word travel cost `c(i,j) = ||x_i - x_j||₂`, the EMD/transportation linear program, the WCD and RWMD lower bounds with prefetch-and-prune, the eight-dataset kNN results (0.42 of BOW error, best on six of eight), limitations around out-of-vocabulary terms, and a closing note on relevance to this project's logit-free document-distance use case.

3. **Task - Document segmentation notebook** (v0.1.0): Built and ran first notebook `notebooks/01-kj-document-segmentation.ipynb` splitting a source PDF into statements with the SAT segmenter on GPU<br>
   **Result**: Created via `datascience:notebook` standards - section order, GPU-selection cell, sectioned Rich config render with Device block, mandatory section overviews. Uses wtpsplit `SaT("sat-3l-sm")` (3-layer subword XLM-R, the PyTorch/GPU path) instead of the `stellars/sat-3l-sm-openvino-int8` build per request. Installed `wtpsplit 2.2.1` into the conda kernel env (resolved cleanly against transformers 5.10.1, no torch in the empty uv venv). GPU ordering differed between nvidia-smi and torch, so pinned `CUDA_DEVICE_ORDER=PCI_BUS_ID` + `CUDA_VISIBLE_DEVICES=2` to isolate the RTX 5000 Ada (cc 8.9) over the Blackwell 5090. Executed end-to-end via nbconvert (logged to `logs/01-document-segmentation.log`): 6 pages → 10,043 chars → 163 statements written to `data/interim/01-statements.parquet`, 0 errors.

4. **Task - README rewrite** (v0.1.0): Replaced the copier boilerplate `README.md` with a project-accurate overview of the WMD / optimal-transport approach<br>
   **Result**: Rewrote the intro to frame the logit-free document-distance goal, added a "Why this exists" section motivating statement-level optimal transport over whole-document cosine (position-invariance, granularity, interpretable alignment for added/dropped content), and a three-stage Approach (segment → embed → compare with unbalanced OT). Listed the stage-1 notebook and linked the WMD paper digest under References; kept the copier Quick Start, Makefile, Best Practices, and Project Organization sections. Added the mandatory `logs/README.md` describing `01-document-segmentation.log` per the background-job logging rule.

5. **Task [Extended] - mmBERT INT8 SmoothQuant notebook** (v0.1.0): Built notebook `00-kj-mmbert-int8-smoothquant.ipynb` quantizing any mmBERT checkpoint to INT8 with SmoothQuant and measuring quality<br>
   **Result**: Stage-0 model-prep notebook for the statement encoder. Post-training-quantizes `jhu-clsp/mmBERT-base` (multilingual ModernBERT, 307M params - the large vocab embedding dominates the 149M backbone) to W8A8 INT8, measuring how far INT8 perturbs the embedding geometry. Implemented SmoothQuant inline: forward-pre hooks collect per-input-channel activation maxima on a disjoint calibration corpus, the smoothing scale `s = act^alpha / weight^(1-alpha)` (alpha 0.5) folds into the preceding `LayerNorm` (divide) and the `Linear` input columns (multiply) - identity in FP, but it flattens the activation outliers that wreck naive INT8. INT8 is fake-quantized (per-channel symmetric weights, per-tensor dynamic activations) via a `QuantLinear` wrapper so quality is measured without INT8 kernels. Quality = Pearson/Spearman of pairwise cosine similarities vs FP32 over a built-in 26-sentence eval corpus, plus a naive-INT8 ablation: SmoothQuant 0.924 Pearson vs naive 0.840 (+0.083), 43 LayerNorm→Linear folds (layer-0 attn_norm is Identity, skipped). Per the user's follow-up, generalized to any mmBERT (original or fine-tuned, Hub id or local path) via `AutoModel` encoder extraction, a `model_type=="modernbert"` guard, FP32 load, and try/except folds. ModernBERT caveats handled: `reference_compile=False` + `attn_implementation="eager"` avoid the first-forward `torch.compile` hang; ran on the RTX 5000 Ada (idx 2). Verified end-to-end via nbconvert (logged to `logs/00-mmbert-int8-smoothquant.log`, 0 errors); wrote `reports/00-mmbert-int8-smoothquant-metrics.json` and `models/00-mmbert-smoothquant-int8.pt`.

6. **Task [Extended] - Improve mmBERT quantization + real OpenVINO INT8** (v0.1.0): Reworked notebook `00` from a per-tensor fake-quant study into an evidence-driven quantization study plus a deployable OpenVINO INT8 artifact (planned + approved via plan mode)<br>
   **Result**: Part A (PyTorch/GPU simulation) added per-token dynamic activation quant alongside per-tensor, a SmoothQuant alpha sweep, a per-component ablation, and in-domain evaluation on notebook 01's 163 statements (`data/interim/01-statements.parquet`, with synthetic fallback). The decisive finding: per-token activation quant is the dominant lever - naive per-tensor 0.929 Pearson vs naive per-token 0.998; SmoothQuant adds only ~0.001 once activations are per-token (per-row scaling already tames the outliers SmoothQuant targets). Best alpha 0.6 (sweep flat ~0.998), all four projections ~0.999 solo so mixed precision was not triggered; chosen config (alpha 0.6, per-token, full INT8) reaches in-domain Pearson 0.9987 vs the prior notebook's 0.924. Part B (OpenVINO/CPU) deploys for real: `optimum-intel`'s OV exporter rejects ModernBERT, so used `openvino.convert_model` directly on the torch module (architecture-agnostic graph trace) then `nncf.quantize(model_type=TRANSFORMER, AdvancedSmoothQuantParameters(matmul=best_alpha))`. The deployed INT8 reaches Pearson 0.956 / Spearman 0.983 / emb-cos 0.992 vs FP32, 1.41x faster (29.1→20.6 ms/sentence CPU) and 1.98x smaller (615→310 MB), saved to `models/00-mmbert-openvino-int8/`. Honest gap surfaced: the simulation's 0.999 is an upper bound from per-token dynamic activations, but NNCF's OV INT8 runtime uses per-tensor static activation quant, so real fidelity lands at 0.956 - reaching the simulated peak in production needs a per-token-dynamic INT8 runtime (ONNX Runtime / torch.ao), not OpenVINO default. De-risked both hard steps in throwaway scripts before baking into the notebook. Side effect: installing `optimum-intel[openvino]` (openvino 2026.2.1, nncf 3.2.0) downgraded transformers 5.10.1 → 5.0.0 env-wide; mmBERT still loads, verified. Wrote `reports/00-mmbert-bestconfig.json` + expanded metrics JSON; nbconvert 0 errors.
