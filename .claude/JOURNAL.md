# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - Project initialization** (v0.1.0): Initialized Claude Code configuration for the `docdistance-estimator` project scaffolded from copier-data-science v1.3.9<br>
   **Result**: Created `.claude/CLAUDE.md` importing workspace-level configuration from `/home/lab/workspace/.claude/CLAUDE.md` with the mandatory reinforced bans (no auto tags/version/publish/commits, Makefile-first installs) and the append-only journal rules. Added project context describing the Word Mover's Distance / Optimal Transport purpose - meaningful document distance for agentic conversion/extraction pipelines that run through frontier models without access to logits for KL divergence. Documented the stack (Python 3.13, `uv`, `typer`, `loguru`, `ruff`, `pytest`) and strengthened rules for notebook standards, background-job logging, and optimal-transport metric correctness. Project already had a local git repo from copier (no commits yet), so git init was skipped.

2. **Task - WMD paper digest** (v0.1.0): Processed the source PDF `[paper] From Word Embeddings To Document Distances.pdf` into a markdown digest<br>
   **Result**: Extracted all 10 pages of the Kusner et al. 2015 ICML paper with `pdfplumber` (poppler was unavailable for direct PDF rendering) and wrote `references/papers/from-word-embeddings-to-document-distances.md` following modus primaris. The digest covers the BOW/TF-IDF distance problem, the nBOW representation, the word travel cost `c(i,j) = ||x_i - x_j||₂`, the EMD/transportation linear program, the WCD and RWMD lower bounds with prefetch-and-prune, the eight-dataset kNN results (0.42 of BOW error, best on six of eight), limitations around out-of-vocabulary terms, and a closing note on relevance to this project's logit-free document-distance use case.

3. **Task - Document segmentation notebook** (v0.1.0): Built and ran first notebook `notebooks/01-kj-document-segmentation.ipynb` splitting a source PDF into statements with the SAT segmenter on GPU<br>
   **Result**: Created via `datascience:notebook` standards - section order, GPU-selection cell, sectioned Rich config render with Device block, mandatory section overviews. Uses wtpsplit `SaT("sat-3l-sm")` (3-layer subword XLM-R, the PyTorch/GPU path) instead of the `stellars/sat-3l-sm-openvino-int8` build per request. Installed `wtpsplit 2.2.1` into the conda kernel env (resolved cleanly against transformers 5.10.1, no torch in the empty uv venv). GPU ordering differed between nvidia-smi and torch, so pinned `CUDA_DEVICE_ORDER=PCI_BUS_ID` + `CUDA_VISIBLE_DEVICES=2` to isolate the RTX 5000 Ada (cc 8.9) over the Blackwell 5090. Executed end-to-end via nbconvert (logged to `logs/01-document-segmentation.log`): 6 pages → 10,043 chars → 163 statements written to `data/interim/01-statements.parquet`, 0 errors.

4. **Task - README rewrite** (v0.1.0): Replaced the copier boilerplate `README.md` with a project-accurate overview of the WMD / optimal-transport approach<br>
   **Result**: Rewrote the intro to frame the logit-free document-distance goal, added a "Why this exists" section motivating statement-level optimal transport over whole-document cosine (position-invariance, granularity, interpretable alignment for added/dropped content), and a three-stage Approach (segment → embed → compare with unbalanced OT). Listed the stage-1 notebook and linked the WMD paper digest under References; kept the copier Quick Start, Makefile, Best Practices, and Project Organization sections. Added the mandatory `logs/README.md` describing `01-document-segmentation.log` per the background-job logging rule.
